### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
```ruby
### Testing task 2 code:

# Carry out dynamic testing on the code below.
# Correct the errors below that you spotted in task 1.

require_relative('card.rb')
class CardGame


  def checkforAce(card)
    if card.value = 1
      # requires a ==
      return true
    else
      return false
    end
  end

  dif highest_card(card1 card2)
  #dif is a typo should be def
  if card1.value > card2.value
    return card.name
    # changed name to value
  else
    card2 # added .value
  end
end
end
# additional end in line 32 not required.
def self.cards_total(cards) #self not required
  total
  # need an = 0 in total
  for card in cards
    total += card.value
    return "You have a total of" + total
  # needs to return cards value total so total added into string.
  end
end


```
